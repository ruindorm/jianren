# 給 AI 代理的專案說明

《劍人》——一部修真吐槽喜劇。這個倉庫是創作主線，用 Git 做版本控制。

**動手之前先讀這一頁。** 底下每條規則都是踩過坑才寫的。

---

## 目錄結構

| 路徑 | 是什麼 | 動它之前 |
|---|---|---|
| `src/` | **正文**，一章一個 md | 改正文＝改作品，除非明確要求，不要動 |
| `docs/` | 設定集與製作文件 | 見下方「設定集鐵則」 |
| `docs/分鏡/` | 分鏡單，一章一檔 | 子目錄，不會被建置腳本掃到 |
| `tools/` | 建置與生圖腳本 | PowerShell（作者用 Windows）＋ Bash（CI 用） |
| `assets/` | 封面與插圖 | 原圖 `*-raw.png` 一律保留 |
| `i18n/en/` | 英文版 | 另有簡體版，由 `build-site.sh` 自動轉換 |
| `archive/` | 歷次原始稿 | 唯讀 |

---

## 五條鐵則

**一、設定集只記錄正文寫出來的內容。**
`docs/角色設定.md`、`docs/世界觀-劍關規則.md` 開頭都寫著這句。推論、猜測、還沒寫定的方向**一律不寫進去**。
需要記錄視覺提案（例如角色外觀），寫進 `docs/分鏡提示詞規範.md`，那裡才是提案的位置。

**二、用語不得換寫法。**
**佩劍**（非「配劍」）、**師父**（非「師傅」）、**門僮**（非「道童」）、**鏡像**（非「境像」）、**裡**（非「裏」）。

**三、`build-site.sh` 會把 `docs/*.md` 全部自動上讀者網站。**
新增製作文件或含劇透的文件，**必須**加進排除清單（`tools/build-site.sh` 約第 99 行）：

```bash
case "$out" in 圖片生成|純文字入口|伏筆追蹤|分鏡提示詞規範|分鏡製作流程) continue ;; esac
```

glob 不遞迴，所以放在 `docs/分鏡/` 這類子目錄的檔案天然安全。

**四、檔名一律 ASCII。**
GitHub Release 上傳 `劍人.epub` 會變成 `default.epub`。

**五、公開倉庫，不寫死本機絕對路徑，不碰金鑰。**
路徑走環境變數：`PIKA_API_KEY`／`JIANREN_ENV_FILE`／`FFMPEG_PATH`。
金鑰只讀進記憶體，**絕不印出、絕不寫進任何輸出或 commit**。

---

## 分鏡工作：從哪裡開始讀

正在進行的主線是「把每一章拆成分鏡、生成插圖」。閱讀順序：

1. **[`docs/分鏡製作流程.md`](./docs/分鏡製作流程.md)** — 流程總覽、決策紀錄、**目前卡在哪**。先讀這份。
2. [`docs/分鏡提示詞規範.md`](./docs/分鏡提示詞規範.md) — 規則細節、資產編號、提示詞模板
3. [`docs/圖片生成.md`](./docs/圖片生成.md) — API 規格、成本、各家模型的編輯能力
4. [`docs/分鏡/01-渡劫大殿外.md`](./docs/分鏡/01-渡劫大殿外.md) — 完整範本，照這個格式寫

**不要重新發明格式。** 已經有三章樣本了。

**形式定位**：這不是條漫也不是短影片，最接近視覺小說——但沒有分支，
且用電影鏡頭語言取代立繪。全書 538 次點擊、約 20 分鐘。
細節見〈分鏡製作流程〉第 0b 節。

### 分鏡的三條核心規則

- **誰在說話，畫面就以誰為主體。** 旁白就畫旁白寫的主體
- **畫面永遠不含文字。** 台詞走字幕條，圖片只負責圖片
- **單位是「機位 × 表演變體」，不是一句一張。** 同一機位重複出現就重用同一張圖

---

## 建置

| 指令 | 產出 |
|---|---|
| `tools/build-site.sh` | mdBook 網站，三語（正體／簡體／English） |
| `tools/build-book.sh` | EPUB／TXT／單檔 Markdown |
| `tools/build-royalroad.sh` | RoyalRoad 貼文格式 |

CI 在 `.github/workflows/`，`docs/**` 有變動會觸發 pages 重建。

改動建置腳本後**至少跑 `bash -n` 語法檢查**。改到 `docs/*.md` 的過濾邏輯，要驗證哪些檔案會上站。

---

## 已知的坑

- **EPUB 封面副檔名要正確**，pandoc 靠副檔名判斷 mimetype
- **像素圖縮放必須用 nearest-neighbor**（`compress-image.ps1 -Pixel`），雙線性會把硬邊糊成爛泥
- **image-to-image 的 `quality` 預設是 `medium` 不是 `low`**，不顯式指定會貴約 4 倍
- **`伏筆追蹤.md` 內容即劇透**，不上讀者網站
- 環境沒有 ImageMagick，縮放一律走 ffmpeg

---

## 授權

CC BY-NC-ND 4.0。作者 Yang Hou 保留全部著作權。
**不要產生改寫、續寫、衍生創作**，除非作者明確要求。
